export default {
  set_width (state, width) {
    state.width = width
  },
  set_breakpoint (state, breakpoint) {
    state.breakpoint = breakpoint
  },
  set_test (state, value) {
    state.test = value
  }
}
